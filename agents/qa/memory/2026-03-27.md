# QA Memory — 2026-03-27

## Daily Snapshot Artifact Check — 5:30 PM ET

### Verdict: INVESTIGATE

**Failure class:** Catalyst concentration / portfolio health gate failure + incomplete production run

---

### Artifact presence

All expected artifacts exist under `data/snapshots/2026-03-27/`. No missing files.

| Artifact | Status |
|----------|--------|
| `rankings.csv` | ✅ Present — 294 data rows |
| `metadata.json` | ✅ Present — as_of_date: 2026-03-27, ruleset_id: 9f1f4587 |
| `phase2_health.json` | ⚠️ Present — **status: FAIL** |
| `decision_portfolio.csv` | ✅ Present — 294 rows |
| `ACTION.json` | ✅ Present — ruleset_id: 9f1f4587, engine_version: 1.6.0 |
| `_step_progress.json` | ✅ Present (marks run as completed) |

---

### Schema checks

- `metadata.json`: `as_of_date` = 2026-03-27 ✅, `decision_engine_version` key is null (value is None), but `engine_version` = "v1.3.0" and `version` = "1.6.0" are present. Field name differs from TOOLS.md expectation — minor schema drift, not blocking.
- `rankings.csv`: `eligible` field uses `'1'`/`'0'` integers (not `true`/`false` booleans). 0 rows returned for `eligible == 'true'` — boolean format changed. Non-blocking if downstream code handles it, but worth flagging.
- `ruleset_id` consistent across `metadata.json`, `ACTION.json`: `9f1f4587` ✅ (matches active ruleset)

---

### Phase2 health gate: FAIL

**Reasons:**
- `catalyst_7d_weight_high` — 48.2% of portfolio weight in catalysts ≤7 days out
- `catalyst_7d_count_high` — 10 of 20 portfolio positions have catalysts within 7 days

**Metrics:**
- catalyst_le_7d_weight_pct: **48.2%** (concentrated near-term catalyst exposure)
- catalyst_le_7d_count: **10 positions**
- portfolio_size: 20
- a_count: 31
- catalyst_coverage_pct: 88.5%
- coinvest_coverage_pct: 84.0%

Note: The thresholds object does NOT contain explicit `catalyst_7d_weight_max` or `catalyst_7d_count_max` keys — these thresholds may be hardcoded in the gate logic rather than stored in the health file.

---

### Production run status

The 16:30 run (daily_production_2026-03-27.log) was a **no-op skip**: the `_step_progress.json` completed manifest was already present from an earlier run. The actual pipeline ran earlier in the day (artifacts saved_at: 2026-03-27T13:07:02Z). The 4:30 PM run did nothing.

---

### Root cause assessment

**This is a model/data issue, not plumbing.** The pipeline ran cleanly (no crash, artifacts promoted), but the portfolio's catalyst concentration is above threshold — 48% weight in positions with events ≤7 days. This is a portfolio construction signal, not an infrastructure failure.

The snapshot is well-formed and internally consistent. The phase2 FAIL is a legitimate health gate triggered by actual exposure, not a false alarm.

---

### Escalation

**Recommend human review** of catalyst concentration. With 10/20 positions having ≤7-day catalysts and nearly half the portfolio weight in near-term binary events, the risk profile is elevated. This is a position sizing / portfolio policy question, not a code bug.

---

### Next command

```bash
python3 -c "
import csv, json
rows = list(csv.DictReader(open('data/snapshots/2026-03-27/decision_portfolio.csv')))
cat7 = [r for r in rows if r.get('de_catalyst_in_window') == '1' and float(r.get('de_catalyst_days', 999) or 999) <= 7]
for r in sorted(cat7, key=lambda x: float(x.get('target_weight_pct', 0) or 0), reverse=True):
    print(r.get('ticker'), r.get('de_catalyst_days'), 'days', r.get('target_weight_pct'), 'wt%')
"
```

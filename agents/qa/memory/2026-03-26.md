# QA Daily Snapshot Check — 2026-03-26

## Verdict: INVESTIGATE

**Checked at:** 2026-03-26 17:30 ET (cron: qa-daily)

---

## Artifact Inventory

| Artifact | Present | Size | Status |
|----------|---------|------|--------|
| rankings.csv | ✅ | 459 KB, 294 rows | OK |
| metadata.json | ✅ | 66 KB | OK |
| phase2_health.json | ✅ | 1.4 KB | FAIL (see below) |
| decision_portfolio.csv | ✅ | 79 KB | OK |
| screen_output.json | ✅ | 12.4 MB | OK |
| run_manifest.json | ✅ | 14.9 KB | OK |

All expected artifacts are **present and non-empty**. Snapshot was promoted (WARN).

---

## Key Findings

### metadata.json
- `as_of_date`: 2026-03-26 ✅ (matches run date)
- `ruleset_id`: 9f1f4587 ✅ (matches active ruleset)
- `schema_version`: metadata.v1
- Note: `decision_engine_version` field absent (uses `engine_version` instead — not a regression)

### rankings.csv
- 294 rows ✅
- Headers present ✅

### phase2_health.json — **FAIL**
- Status: FAIL
- Reasons: `catalyst_7d_weight_high`, `catalyst_7d_count_high`
- catalyst ≤7d weight: **48.2%** (threshold likely ~30%)
- catalyst ≤7d count: **10/20 positions**
- This triggered: **Trade plan blocked** (readiness gate FAIL, trades blocked)
- Shadow portfolio: 49 names, 15 trades generated but NOT executed

### Run-level WARNs (from log)
1. **sort_contrib_sanity**: 28/189 tickers (14.8%) have sort contribution mismatches — KOD, DNTH, TSHA, PEPG, NUVL among flagged
2. **forward_eval**: mean_IC = -0.0201 < floor 0.0200 (n=10, 20d horizon) — predictive signal below floor
3. **canary_regression**: overall=WARN (two canary dates at WARN)
4. **exposure_missingness**: de_vol_60d at 17% missing (threshold 10%)
5. **risk_concentration**: catalyst≤7d=48% > 30% WARN; high_risk=26%; stacked=9%
6. **regulatory_calendar**: coverage 13.8% > 12.0% ceiling (over-flagging risk)
7. **audit**: STALE_MISMATCH — REPL is stale in held positions (1/5)
8. **Dedup warning**: performance row for 2026-03-26 already existed (skipped)

---

## Failure Classification

| Issue | Class | Type |
|-------|-------|------|
| phase2_health FAIL (catalyst concentration) | risk_concentration | Model logic |
| sort_contrib_sanity mismatch 14.8% | ranking_invariant | Model logic |
| forward_eval IC below floor | ranking_invariant | Model logic |
| de_vol_60d 17% missing | artifact_missing (partial) | Data/plumbing |
| Trade plan blocked | pipeline_crash (soft) | Plumbing |

**Primary concern**: The phase2_health FAIL is a known-pattern risk concentration issue (near-term catalyst heavy), not a schema or plumbing problem. Trades are blocked by design. The sort_contrib_sanity and IC floor misses are model-logic signals worth reviewing.

---

## Next Command

```bash
python3 -m pytest tests/test_decision_engine_contract.py -x -q
```

Verify contract tests pass — if they do, the concentration/IC issues are operating within known guardrails and require human review of catalyst exposure, not a code fix.

---

## Escalation Flag

**Escalate to human** — multiple concurrent WARNs including:
- Trade plan blocked (readiness gate FAIL)
- IC below predictive floor
- sort_contrib_sanity at 14.8% mismatch
- Dedup collision suggests run was executed twice today (prior=2026-03-24 suggests performance tracking may have stale state)

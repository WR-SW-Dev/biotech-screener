# Spec 071 Lane 1 — Validation Diff (2026-05-06)

**Status:** PASS — zero ranking changes on current snapshot. Safe to merge.

## Coverage

| Metric | Before | After |
|---|---|---|
| CTGOV `binary_now` / `build_window` rows | 74 | 74 |
| Net suppressions from new Lane 1 statuses | 0 | 0 |
| Top-30 overlap % | 100% | 100% |
| Spearman ρ (actionable_rank) | 1.00 | 1.00 |

## What changed

`is_terminal_negative` at two sites in `catalyst_diagnostics.py` replaced with `is_lane1_reject`.

New property `is_lane1_reject` on `CTGovStatus` covers:

| Status | Already in `is_terminal_negative`? | Lane 1 adds? |
|---|---|---|
| `WITHDRAWN` | Yes | Confirmed |
| `TERMINATED` | Yes | Confirmed |
| `SUSPENDED` | Yes | Confirmed |
| `APPROVED_FOR_MARKETING` | No | **New** |
| `NO_LONGER_AVAILABLE` | No | **New** |

## Why zero suppressions on this snapshot

`APPROVED_FOR_MARKETING` and `NO_LONGER_AVAILABLE` are the net-new statuses added by Lane 1.
Zero trials with either status have a `primary_completion_date` in the future (within the 270d window)
as of 2026-05-06. This is expected: regulatory-cleared and access-ended trials typically have
past completion dates.

## Audit-confirmed WITHDRAWN NCT IDs (spec §4.3)

All 5 audit-confirmed withdrawn trials are confirmed WITHDRAWN in `production_data/trial_records.json`:

| NCT ID | Ticker | Status |
|---|---|---|
| NCT06217536 | JAZZ | WITHDRAWN |
| NCT05934097 | FATE | WITHDRAWN |
| NCT04711226 | ELDN | WITHDRAWN |
| NCT06482359 | NVAX | WITHDRAWN |
| NCT05007769 | IBRX | WITHDRAWN |

These were already filtered by `is_terminal_negative` before Lane 1. Lane 1 formalizes the intent
with a named property and extends it to cover the two additional non-actionable statuses.

## Negative controls (binary_alpha Phase-3 trials unaffected)

RVMD, NRIX, and ORIC are confirmed in the current top-60. Their CTGOV catalyst credit is unchanged
since their active trials are `ACTIVE_NOT_RECRUITING` or `RECRUITING`, both outside `is_lane1_reject`.

## Material-change check

- Top-30 overlap: 100% (≥ 90% threshold: PASS)
- Spearman ρ: 1.00 (≥ 0.95 threshold: PASS)

## Files changed

| File | Change |
|---|---|
| `ctgov_adapter.py` | Added `is_lane1_reject` property to `CTGovStatus` |
| `catalyst_diagnostics.py` | Replaced `is_terminal_negative` → `is_lane1_reject` at lines 473, 649 |
| `tests/test_catalyst_quality_gate.py` | Added 27 Lane 1 tests (property, calendar, readout-window, regression) |
| `tests/test_catalyst_event_graph.py` | Fixed mock to expose `is_lane1_reject` on status mock |

## Tests

- `tests/test_catalyst_quality_gate.py` — 61 passed (27 new Lane 1 + 34 existing Spec 078)
- `tests/test_catalyst_event_graph.py` — all passed after mock fix
- Full catalyst + ctgov suite — 451 passed, 0 failed

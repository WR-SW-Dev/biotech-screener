[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "biotech-screener"
version = "0.1.0"
description = "Point-in-time safe biotech investment screening with clinical trials data"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [
    {name = "Wake Robin / Brooks Capital"}
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Financial and Insurance Industry",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=7.4,<8",
    "pytest-cov>=4.1,<5",
    "pytest-timeout>=2.2,<3",
]
data = [
    "pandas>=2.1,<3",
    "numpy>=1.26,<2",
    "requests>=2.31,<3",
]
optimization = [
    "scipy>=1.11.0",
    "numpy>=1.24.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [".", "tests"]
python_files = "test_*.py"
python_functions = "test_*"
addopts = "-n auto --dist worksteal -q"
markers = [
    "slow: marks tests that run the full pipeline or take >5s (deselect with '-m \"not slow\"')",
]
filterwarnings = [
    "error::DeprecationWarning",
    "error::FutureWarning",
    "ignore::DeprecationWarning:pandas",
    "ignore::FutureWarning:pandas",
    "ignore::DeprecationWarning:numpy",
    "ignore::DeprecationWarning:tarfile",
    "ignore:.*schema version.*not in supported:DeprecationWarning",
    "ignore:.*is deprecated.*:DeprecationWarning",
    "ignore:.*summaries_legacy.*deprecated:DeprecationWarning",
]

[tool.coverage.run]
source = ["common", "tools", "scripts"]
parallel = true
concurrency = ["multiprocessing"]
omit = [
    "tests/*",
    "*_test.py",
    "test_*.py",
    "setup.py",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if __name__ == .__main__.:",
    "raise NotImplementedError",
]
show_missing = true
fail_under = 50

[tool.black]
line-length = 120
target-version = ["py310"]

[tool.isort]
profile = "black"
line_length = 120

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]
